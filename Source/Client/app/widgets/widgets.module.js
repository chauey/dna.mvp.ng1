(function () {
    'use strict';

    angular.module('app.widgets', []);

    // The following widgets are from John Papa's CodeCamper project http://cc-ng-z.azurewebsites.net/,
    // we changed 'cc' prefix to 'dna' because "we do not want to put CC letters in every project we work on":
    // - dnaImgPerson (ccImgPerson)
    // - dnaSidebar (ccSidebar)
    // - dnaWidgetClose (ccWidgetClose)
    // - dnaWidgetMinimize (ccWidgetMinimize)
    // - dnaScrollToTop (ccScrollToTop)
    // - dnaSpinner (ccSpinner)
    // - dnaWidgetHeader (ccWidgetHeader)
    // - dnaWip (ccWip)
})();